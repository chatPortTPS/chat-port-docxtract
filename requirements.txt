# Índice extra para PyTorch CPU
--extra-index-url https://download.pytorch.org/whl/cpu

# Document processing
pymupdf==1.23.14
openpyxl==3.1.2
camelot-py[cv]==0.10.1
tabula-py==2.8.2

# Data manipulation
pandas==2.1.4
numpy==1.26.4
python-pptx==1.0.2
python-docx==1.2.0


# Networking and messaging
stomp.py==8.1.0
paramiko==3.3.1
elasticsearch==8.11.1

# Environment configuration
python-dotenv==1.0.0

# PyTorch CPU-only (evita paquetes CUDA)
torch==2.2.0+cpu
torchvision==0.17.0+cpu

# NLP stack
sentence-transformers>=2.4.0
transformers>=4.38.0
huggingface-hub>=0.20.0

# LangChain (deja que resuelva sus sub-módulos compatibles)
langchain==0.1.16
langchain-text-splitters      # sin versión -> compatible con langchain 0.1.16
langchain-community           # sin versión -> compatible con langchain 0.1.16
# (No fijar langchain-core ni langsmith manualmente)
 
# Dependencias para extracción de palabras clave

# KeyBERT - Basado en BERT para extracción de palabras clave
keybert>=0.8.0

# YAKE - Yet Another Keyword Extractor
yake>=0.4.8

# RAKE - Rapid Automatic Keyword Extraction
rake-nltk>=1.0.6

# scikit-learn para TF-IDF
scikit-learn>=1.3.0

# Dependencias adicionales
nltk>=3.8.0 


spacy>=3.7.0,<3.8.0
es_core_news_md @ https://github.com/explosion/spacy-models/releases/download/es_core_news_md-3.7.0/es_core_news_md-3.7.0-py3-none-any.whl
